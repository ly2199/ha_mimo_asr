{
  "config": {
    "title": "Mimo Speech-To-Text",
    "step": {
      "user": {
        "title": "Mimo Speech-To-Text",
        "description": "Enter your Mimo API Key",
        "data": {
          "api_key": "API Key"
        }
      }
    },
    "abort": {
      "single_instance_allowed": "Only one instance is allowed"
    }
  },
  "options": {
    "step": {
      "init": {
        "title": "Recognition options",
        "description": "Tune when recognition ends and the API request timeout.",
        "data": {
          "silence_seconds": "Trailing silence (seconds)",
          "request_timeout": "API request timeout (seconds)"
        }
      }
    }
  }
}
